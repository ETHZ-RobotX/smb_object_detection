person:
  max_depth : 0.2
  unique : x

bicycle:
  max_depth : 0.2
  unique : y

car:
  max_depth : 0.15  
  unique : xy

clock:
  max_depth : 0.3
  unique : xy

bottle:
  max_depth : 0.3
  unique : xy

umbrella:
  max_depth : 0.4
  unique : xy